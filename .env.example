# Absolute path to the folder that holds the timing backend's .txt feed file(s).
# The live feed (/live) lists and reads .txt files from here. LOCAL ONLY — never
# set this in a deployed environment.
FEED_DIR=/Users/Noah/Coding/lion-central/.scratch-feeds/
